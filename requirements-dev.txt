# Dev/test dependencies for zBots itself.
# Production runs inside zBots' own standalone container (see Dockerfile),
# which already installs FastAPI/uvicorn/httpx/python-multipart directly.
# This file exists so the test suite (tests/, mocked at the backend.main
# boundary -- no live Hermes instance needed) can run without building that
# whole image, in CI or locally.
fastapi
uvicorn
httpx
python-multipart
mcp==2.0.0
pywebpush
pytest
pytest-asyncio
